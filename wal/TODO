- licence

x wal.postToServer/getFromServer always encode as json

x move dispatcher into wal/wal
x move test files into wal/wal/test
x move wal/wal into urnest/wal

x rework response type handling:
  x make content given to Response always bytes (check it)
  x then once we have a Response object we can return it directly
  
- dispatch to function
  x test cookies
  x get
  / post
    x ajax
    x form
    - mime
  x validate returned cookies
  x test non-public+cookies
  
  - use urllib.parse.parse_sq
  - use cgi.parse_multipart

x test ClientError

x JsonSchema:

- automate all tests

- wal/__init__.py use PushPath so only add dir for this module

- wal.js doco postToServer onwards
