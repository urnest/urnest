+env 'an environment variable to set when running test, e.g. TEST_PORT=9999'? => :ls;

:sh-profile 'shell profile file (for use with sh/bash source command / .-syntax)' => :FILE;

EXEC (sh-profile.sh) (+env) => (:sh-profile);
