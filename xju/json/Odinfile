%all==%all.tree:leaves

%all.tree==<<
%tests.tree

%tests.tree == <<
()+cmd=(test-parse.cc+(../..%cxx-opts):auto.cxx.exe):exec.output

%tags==%all.list-of-tags:merged-tags

%all.list-of-tags==<<
.:merged-tags
